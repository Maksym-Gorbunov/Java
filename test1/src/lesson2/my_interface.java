package lesson2;

// class     -> class           extends
// interface -> class           implements
// interface -> interface       extends


//public interface == abstract public class,
// all intern methods and members(variables) are abstract by nature(automatically)

//abstract public class my_interface {
public interface my_interface {
    int some_method();  // don't need to initialize
}
